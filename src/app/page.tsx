// Instead of using redirects which can cause issues with route groups,
// we'll export the Home component directly which is what we want to show at the root URL
export { default } from './(home)/page';
